fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
numpy==2.2.1
httpx==0.28.1
# optional: dense embeddings. Without these the hashed encoder is used.
sentence-transformers==3.3.1; python_version < "4.0"
# optional: pgvector backend. Without DATABASE_URL the in-memory index is used.
psycopg[binary]==3.2.3
pgvector==0.3.6
# optional: LLM narration over the already-selected recommendations
anthropic==0.42.0
